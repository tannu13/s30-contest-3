# Server

Express + WebSocket backend for the Remote Controlling Claude Code contest.

All routes currently return **501 Not Implemented**. Implement session management, persistence, and WebSocket terminal forwarding in:

- `src/routes/`
- `src/services/`
- `src/repositories/`

See the root `README.md` for the full contest specification.
