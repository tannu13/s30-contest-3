# Client

Remote terminal frontend for the Remote Controlling Claude Code contest.

**Do not modify this package.** Students implement the backend only.

## Routes

- `/` — landing page with setup instructions
- `/session/:sessionId` — live terminal UI with WebSocket connection

## Development

```bash
bun run dev
```

Runs on port `5173`.
